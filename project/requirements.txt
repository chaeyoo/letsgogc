# RAPV-Assistant 의존성 (Python 3.11+)
# RAG 파이프라인은 외부 ML 라이브러리 없이 순수 파이썬으로 구현되어
# numpy/torch/sentence-transformers 등 무거운 의존성이 없다.
fastapi==0.139.0
uvicorn==0.50.2
fastmcp==3.4.3
httpx==0.28.1        # API → MCP 서버 헬스체크 ping (fastmcp 전이 의존성이지만 직접 사용이라 명시 고정)
anthropic==0.116.0   # 선택: ANTHROPIC_API_KEY 설정 시 LLM 모드 활성화
pydantic==2.13.4
pydantic-ai-slim[anthropic,mcp]==2.13.0   # 선택: AGENT_BACKEND=pydantic_ai 백엔드
